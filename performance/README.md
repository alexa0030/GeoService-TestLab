# Performance tests

Install Apache JMeter 5.6.3, start the stack, then run
`bash scripts/run_performance.sh`. Thread count and loop count are supplied as
JMeter properties. Raw `.jtl` files are ignored by Git; analyzed summaries and
figures are generated from real runs only.

