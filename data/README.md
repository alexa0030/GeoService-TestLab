# Sample data

`sample.geojson` is a small synthetic urban–rural gradient dataset around Shanghai.
It is deliberately synthetic and safe to publish. `init.sql` loads equivalent
features automatically when the PostGIS volume is first created.

